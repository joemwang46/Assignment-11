1. place all files in the same directory
2. run the main.py file. This will run feature importance, model evaluation, and backtesting. Graphs will be displayed out results
3. to run tests/coverage, run "coverage run -m pytest" in command line. all 15 test cases will be passed.